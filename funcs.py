def sum_numbers(numbers):
    total = 0
    for number in numbers:
        total += number
    return total


def diff_numbers(numbers):
    total1 = 0
    for number in numbers:
        total1 -= number
    return total1
